#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int a, b;

    scanf("%d", &a);
    scanf("%d", &b);

    printf("The sum of the numbers is: %d\n", a + b);
    printf("The product of the numbers is: %d\n", a * b);
    return 0;
}
